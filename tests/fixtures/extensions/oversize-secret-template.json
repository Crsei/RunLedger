{
  "schemaVersion": 1,
  "mcpServers": {
    "secret-fixture": {
      "transport": "streamable-http",
      "url": "https://example.invalid/mcp",
      "headers": {
        "Authorization": "Bearer ${RUNLEDGER_TEST_SECRET}"
      }
    }
  }
}
