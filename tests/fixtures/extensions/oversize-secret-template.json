{
  "_purpose": "M0 oversize/secret fixture; bounded reads and secret redaction are pending",
  "schemaVersion": 1,
  "template": "${RUNLEDGER_SECRET_SHOULD_NOT_BE_PERSISTED}",
  "declaredBytes": 999999
}
