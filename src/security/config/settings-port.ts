/** Security domain facade; durable raw I/O remains in the canonical storage adapter. */

export {
	SecuritySettingsPort,
	type SecuritySettingsInspection,
	type SecuritySettingsPortOptions,
	type SecuritySettingsScope,
	type SecuritySettingsUpdate,
} from "../../storage/security-settings-port.ts";
