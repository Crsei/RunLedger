# 锁文件变更需要 PI.allow_lockfile_change 之类的机制,初期不强制。
# 避免运行 install 时无意触发 lifecycle scripts(参考 pi 的实践)。
ignore-scripts=false
# @opentui/core@0.4.5 固定 peer web-tree-sitter@0.25.10，而 Bash AST
# 固定官方 runtime 0.26.11；暂时显式保留该上游 peer 冲突，TUI/AST 测试共同验证 API。
legacy-peer-deps=true
